#include <bits/stdc++.h>
using namespace std;
int main()
{
    int ravi_has_money;
    cout << "ravi_has_money\n";
    cin >> ravi_has_money;

    if (ravi_has_money > 20 && ravi_has_money < 30)
    {0
        cout << "Give it to Ritik\n";
    }
    else if (ravi_has_money > 30 && ravi_has_money < 50)
    {
        cout << "Give it to Rajeshwar\n";
    }
    else
        {
            cout << "Keep with himself";
        }
    return 0;
}
